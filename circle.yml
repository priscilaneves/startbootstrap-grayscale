machine:
  node:
    version: 0.12.0
dependencies:
  override:
    - sudo pip install awscli
test:
  override:
    - grunt
deployment:
  prod:
    branch: gh-pages
    commands:
      - grunt
      - (bash s3cmd.sh)
      - grunt test
